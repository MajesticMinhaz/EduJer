// for splash screen style

import {StyleSheet} from 'react-native';
import {colors} from '../../assets/colors.style.asset';

export const splashStyles = StyleSheet.create({
  container: {
    backgroundColor: colors.primary,
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
